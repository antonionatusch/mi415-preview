#let random_numbers_independence_tests_intro = {
  [
    Recuerde que las dos propiedades más importantes que deben
    satsifacer los números de un conjunto $r_i$ son uniformidad
    e independencia. En la sección anterior comentamos las
    pruebas que buscan determinar si los números del conjunto
    $r_i$ son uniformes. A continuación hablaremos de las
    pruebas estadísticas que tratan de corroborar si los
    números en el intervalor $(0,1)$ son independientes
    o, en otras palabras, si parecen pseudoaleatorios.
    Para probar la independencia de los números de un conjunto
    $r_i$ primero es preciso formular las siguientes hipótesis: \
    $quad H_0: "los números del conjunto" r_i "son independientes"$ \
    $quad H_1: "los números del conjunto" r_i "no son independientes"$ \
    #cite(<garcia2013simpromodel>, supplement: [pg.~41])
  ]
}
